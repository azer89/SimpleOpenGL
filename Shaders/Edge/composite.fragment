#version 460 core

out vec4 FragColor;

in vec2 TexCoords;

uniform sampler2D depthMap;

void main()
{
    vec3 posValue = texture(depthMap, TexCoords).rgb;
    //float depth = posValue.z ;

    FragColor = vec4(posValue, 1.0);
    //FragColor = vec4(vec3(depth), 1.0);
}